name: hyperliquid_dart
description: A Dart SDK for the Hyperliquid DEX — REST API, WebSocket subscriptions, EIP-712 signing, and order management.
version: 0.16.2
repository: https://github.com/Riten-Zone/Hyperliquid-Dart-SDK
issue_tracker: https://github.com/Riten-Zone/Hyperliquid-Dart-SDK/issues
topics:
  - hyperliquid
  - defi
  - trading
  - web3
  - cryptocurrency

environment:
  sdk: ^3.10.0

dependencies:
  http: ^1.6.0
  web_socket_channel: ^3.0.3
  pointycastle: ^4.0.0
  msgpack_dart: ^1.0.1
  convert: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
